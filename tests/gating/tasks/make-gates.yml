platform: linux
image_resource:
  type: docker-image
  source:
    repository: alpine
outputs: 
  - name: gates
run: 
  path: /bin/sh
  args:
    - -exc
    - | 
      stamp=$(date +%s)
      echo "${stamp}" > ./gates/run_${stamp}
